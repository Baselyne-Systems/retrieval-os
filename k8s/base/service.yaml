apiVersion: v1
kind: Service
metadata:
  name: retrieval-os
  labels:
    app: retrieval-os
spec:
  selector:
    app: retrieval-os
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
